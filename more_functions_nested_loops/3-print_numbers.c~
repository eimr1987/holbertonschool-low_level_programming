#include "main.h"

/**
 * print_numbers - imprime numeros del cero al nueve.
 *
 * Return: cero
 */
void print_numbers(void)
{

	char i;

	for( i = '0'; i <= '9'; i++)
	{
		_putchar (i);
	}
	return (0);
}
